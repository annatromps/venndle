default: &default
  adapter: postgresql
  encoding: unicode
  max_connections: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: venndle_development

test:
  <<: *default
  database: venndle_test

production:
  <<: *default
  url: <%= ENV["DATABASE_URL"] %>
